# 依赖改用 ^ 范围（而非 workspace:*）后，pnpm 10 默认不再把 @hyzyn/dsh-* 解析到
# 本地子包，而是从 registry 拉 —— 本地改 packages/kit 不生效，会调试到发布出去的旧代码。
# 开启后：范围匹配到本地 workspace 包版本时仍走 link，开发体验与 workspace:* 一致。
# 背景见 dsh-plugin-kit issue：git 子路径安装因 workspace:* 残留失败。
link-workspace-packages=true
