fix(release): publish-targets 补上 @hyzyn/dsh-docker，并加覆盖度检查防漏配

v0.1.20 的 Release workflow 全绿，但 registry 上没有 @hyzyn/dsh-docker——
发布清单 scripts/publish-targets.mjs 是手工维护的，新增插件包时漏了一行，
而 release-publish-tag.mjs 只是「跳过不在清单里的包」，不会报错。

- 补上 ['packages/docker', '@hyzyn/dsh-docker']（按依赖序放在聚合层之前）
- check-publishable.mjs 增加覆盖度检查：packages/* 下每个非 private 包都必须
  在清单内，否则 CI 与 Release workflow 直接红（已验证漏配时 exit 1）
