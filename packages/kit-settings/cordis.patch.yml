# dsh-plugin-kit/kit-settings bundle patch：把本插件行插入 profile 阵容。
# 作为 profile bundle 层（package.json 的 dsh.bundle.patch 字段）叠加到
# dsh-base 之上；启用方式：
#   dsh plugin --profile web add link:$(pwd)/packages/kit-settings
- insert:
    - id: kit-settings
      name: '@hyzyn/dsh-kit-settings'
