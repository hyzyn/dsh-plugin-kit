# dsh-plugin-kit/profile bundle patch：把本插件行插入 profile 阵容。
# 作为 profile bundle 层（package.json 的 dsh.bundle.patch 字段）叠加到
# 目标 profile：dsh plugin --profile web add link:$(pwd)/packages/profile
- insert:
    - id: profile-manager
      name: '@hyzyn/dsh-profile'
